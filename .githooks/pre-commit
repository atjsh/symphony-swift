#!/bin/sh
set -eu

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

echo "[symphony-build] pre-commit: running tests and first-party coverage gate" >&2
exec swift run --package-path "$repo_root" symphony-build harness
